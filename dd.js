const spans = document.querySelectorAll(
  ".PrimarySelect_options__30565 .PrimarySelect_name__1FZKE"
);

if (spans.length > 0) {
  const firstSpan = spans[0];
  console.log("jensiat:", firstSpan);

  firstSpan.click();
}
